# Database backups

Written every 15 minutes by the `Back up the database` action on `main`. One JSON file per table, one commit per change. Restore with `node scripts/restore.mjs` from `main` — see README.md there.
